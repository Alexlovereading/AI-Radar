# AI 新词雷达 —— 小白使用指南

> 这份文档假设你**没有编程背景**,只是想让这套系统跑起来、看得懂发生了什么。
> 技术细节(数据格式、每个模块怎么写的)在 [CONTRACT.md](./CONTRACT.md) 和 [README.md](./README.md) 里,不需要看那两个也能把系统用起来。

---

## 目录

1. [这个项目到底是什么](#1-这个项目到底是什么)
2. [它是怎么运作的(大白话)](#2-它是怎么运作的大白话)
3. [现状盘点:27 个监控源,哪些能直接用](#3-现状盘点27-个监控源哪些能直接用)
4. [开始之前你要知道的几件事](#4-开始之前你要知道的几件事)
5. [详细步骤:从零到跑起来](#5-详细步骤从零到跑起来)
6. [日常怎么用](#6-日常怎么用)
7. [常见问题 FAQ](#7-常见问题-faq)
8. [以后还能怎么升级(不着急做)](#8-以后还能怎么升级不着急做)

---

## 1. 这个项目到底是什么

一句话:**它是一个自动巡逻的机器人,每 20 分钟去 27 个地方看一眼"有没有新的 AI 模型/产品冒出来",发现了就打分,分数够高就发消息提醒你。**

具体解决的问题(你最早提出来的场景):

- OpenAI 出了个新模型,你想第一时间知道,而不是等它上了新闻。
- 某个平台(比如 OpenRouter)突然多了一个叫 `stealth/ox-alpha` 的神秘模型,还没人讨论,你想比所有人早两步发现它。
- 发现了之后,你想知道"这个东西值不值得我花时间去做一个专题页面抢关键词流量"——而不是每次都要自己判断。

它**不会**帮你做的事(需要你自己判断/动手):

- 不会自动帮你写文章、自动帮你建站发布——它只负责"发现 + 打分",给你一份"这个词值不值得做"的参考数据。
- 不会 100% 保证不漏掉任何一个新模型——第 3 节会诚实地告诉你哪些源现在还抓不到。
- 不会帮你花钱买服务器——后面会解释为什么不需要服务器。

---

## 2. 它是怎么运作的(大白话)

想象成一个 4 步流水线:

```
27 个"侦察兵"分头去看     →   把新发现记下来   →   按规则打分   →   分数够高就通知你
(OpenRouter/OpenAI/Reddit...)   (一个流水账文件)      (0~100+分)      (发到飞书群)
```

**第 1 步 —— 侦察**:27 个小脚本,每个负责盯一个地方(比如"OpenRouter 模型列表"是一个脚本,"OpenAI 官方公告"是另一个)。每次运行,每个脚本都会去看"现在有什么",和"上次看到的"做对比,只把**新出现的东西**记下来。

**第 2 步 —— 记账**:所有新发现的东西,都会追加写进一个叫 `data/events.jsonl` 的文件里,像一本流水账,不会覆盖历史记录。

**第 3 步 —— 打分**:系统会把"同一个东西在不同地方被发现"这件事合并起来算分——比如"OpenAI 官方公告发了 + OpenRouter 也上线了 + 免费",这种会得高分;"只是某个论坛帖子提了一嘴,来源不明"这种得分很低。打分规则就是你之前提的那一套(头部公司发布 +30、免费 +15、一小时内多处独立讨论 +15,等等),写在 `scoring/score.mjs` 里。

**第 4 步 —— 通知 + 生成网站数据**:分数 ≥ 30 分的,会往你配置的飞书群发一条消息。同时,所有打分结果会被整理成一份网站能直接读取的数据文件(`site/data/entities.json`),给你之前设计的那套"模型专题页"模板用。

这一整套流程,**每 20 分钟自动跑一次**,不需要你的电脑开着——运行在 GitHub 免费提供的云端环境里(下面会详细讲怎么配置)。

---

## 3. 现状盘点:27 个监控源,哪些能直接用

图例:✅ 不用你做任何配置,直接能用 　⚠️ 能用,但需要你去申请一个免费/付费的密钥(API Key)才会启用,不申请就是"优雅跳过",不影响其他部分 　❌ 目前这个源抓不到,已知限制,会诚实返回"没发现",不会报错崩溃

### 3.1 模型目录(9 个)

| 源 | 状态 | 说明 |
|---|---|---|
| OpenRouter | ✅ | 官方公开接口,直接能用 |
| Hugging Face | ✅ | 官方公开接口,直接能用(实测已经抓到几十个新模型) |
| Ollama Library | ✅ | 官方没有接口,抓的是网页,已验证能用 |
| fal.ai | ✅ | 官方没公开文档但找到了真实可用的接口 |
| LM Arena | ✅ | 没有官方接口,借用了 Hugging Face 的数据服务,已验证能用(实测抓到 394 个模型) |
| Replicate | ⚠️ | 需要 `REPLICATE_API_TOKEN`(免费注册可申请) |
| Together AI | ⚠️ | 可能需要 `TOGETHER_API_KEY` |
| Fireworks AI | ⚠️ | 需要 `FIREWORKS_API_KEY` + `FIREWORKS_ACCOUNT_ID` |
| Artificial Analysis | ⚠️ | 需要 `ARTIFICIAL_ANALYSIS_API_KEY`(有免费档,每天 100 次够用) |

### 3.2 官方发布源(11 个)

| 源 | 状态 | 说明 |
|---|---|---|
| OpenAI | ✅ | 官方有 RSS 订阅,最稳定的一个 |
| Anthropic | ✅ | 抓官网新闻页,已验证能用 |
| Google DeepMind | ✅ | 抓官网博客,已验证能用 |
| Meta AI | ✅ | 抓官网博客,已验证能用 |
| DeepSeek | ✅ | 抓官网新闻页,已验证能用 |
| Mistral | ✅ | 官方有 RSS 订阅 |
| Moonshot / Kimi | ✅ | 抓官网博客,已验证能用 |
| 智谱 GLM | ✅ | 抓官网新闻页,已验证能用 |
| MiniMax | ✅ | 抓官网博客,已验证能用 |
| Qwen(阿里) | ⚠️ | 官网新版是纯前端渲染抓不到,目前降级用旧版博客的 RSS(能用但内容可能不是最新的) |
| xAI | ❌ | 官网有反爬虫拦截(Cloudflare),目前抓不到,会诚实跳过 |

### 3.3 社区 / 新品源(7 个)

| 源 | 状态 | 说明 |
|---|---|---|
| GitHub Trending | ✅ | 已验证能用 |
| Hacker News | ✅ | 用的官方免费接口,已验证能用 |
| Google Trends(热点趋势) | ✅ | 用的非官方数据源,能用但不保证长期稳定 |
| AppSumo | ⚠️ | 抓网页,今天实测有时能抓到、有时抓不到(网站改版会影响),抓不到时会诚实跳过 |
| Reddit | ⚠️ | 代码逻辑没问题,但在测试环境里被 Reddit 拒绝访问了几次(403),部署到 GitHub 之后网络环境不同,需要你实际跑起来后看日志确认是否正常 |
| Chrome Web Store | ❌ | 官网是纯前端渲染,现有技术抓不到,已知限制 |
| Product Hunt | ⚠️ | 需要 `PRODUCTHUNT_TOKEN`,不申请就跳过 |

**看到这里你可能会问:那些 ❌ 和不稳定的 ⚠️ 怎么办?** 答案是:**不用管**。这套系统的设计原则是"一个源抓不到,不影响其他 26 个源正常工作"。你完全可以先用能跑的这些,以后想补的时候再让人(或者我)去改进对应的脚本。

---

## 4. 开始之前你要知道的几件事

### 需要自己买/租服务器吗?

**不需要。** 这套系统用的是 **GitHub Actions**——GitHub 免费提供的云端小机器人,你把代码传到 GitHub 上,配置一个"每 20 分钟跑一次"的规则,GitHub 就会在它自己的服务器上按时执行,不需要你的电脑开机,也不需要你花钱买云服务器。

网站部分(那套"模型专题页"模板)可以部署到 **Vercel**(专门托管这类网站的免费平台),同样不需要买服务器。

### 需要花钱吗?

基本不需要,但有一个**重要的坑**要注意:

> ⚠️ **GitHub 仓库请选择"Public(公开)",不要选"Private(私有)"。**
> 原因:GitHub Actions 对 Public 仓库完全免费不限量,但 Private 仓库每月只有 2000 分钟免费额度。这套系统每 20 分钟跑一次、一天跑 72 次,如果是 Private 仓库很可能超额产生费用或被暂停。反正抓取的都是各家公司主动公开的信息,选 Public 没有隐私问题。

其他花费:各家 API(Replicate、Together 等)大多有免费额度,不申请也完全能用(对应的源会自动跳过),Vercel 个人项目免费额度对这种小网站也完全够用。

### 需要会写代码吗?

不需要。接下来的步骤全部是"复制命令粘贴到终端"或"在网页上点按钮填表单",不需要你改一行代码。

---

## 5. 详细步骤:从零到跑起来

### Step 0:确认电脑环境没问题

打开"终端"(Terminal)App,输入:

```bash
cd /Users/alex/Desktop/Tools/ai-word-radar
node -v
```

只要显示一个版本号(比如 `v20.x.x` 或更高)就说明没问题,这台电脑已经装好了需要的软件(我之前搭建的时候已经验证过)。

### Step 1:本地先跑一次,建立信心

还在刚才那个终端窗口里,输入:

```bash
npm run radar
```

等大概 30-60 秒,你会看到一堆输出,最后几行类似:

```
[run-all] scraping done in 40.2s
[run-all] total new items: 60
[run-all] scored 96 entities — tiers: {"log-only":96}
[run-all] notified 0/0 entities >= reserve tier
[run-all] site/data/entities.json refreshed
```

这就说明系统真的跑起来了。`notified 0/0` 是正常的——现在因为运行环境是同一台电脑反复测试,发现的大多是新老对比噪音,分数不高很正常,不代表系统坏了。

### Step 2:注册 GitHub 账号(如果还没有)

打开浏览器访问 [github.com](https://github.com),点右上角 "Sign up",按提示注册一个免费账号。

### Step 3:创建仓库,把代码传上去

1. 登录 GitHub 后,点右上角的 "+" 号 → "New repository"
2. **Repository name** 填 `ai-word-radar`(或者你喜欢的名字)
3. 选择 **Public**(注意,不要选 Private,原因见第 4 节)
4. **不要**勾选 "Add a README file"、"Add .gitignore"、"Choose a license" 这几个选项(因为本地已经有文件了,勾选会冲突)
5. 点绿色的 "Create repository"

创建完成后,GitHub 会显示一堆命令。回到终端,确认你还在项目目录下(`/Users/alex/Desktop/Tools/ai-word-radar`),依次执行(把 `你的用户名` 换成你的 GitHub 用户名):

```bash
git remote add origin https://github.com/你的用户名/ai-word-radar.git
git branch -M main
git push -u origin main
```

第一次执行 `git push` 时,可能会弹出浏览器窗口要求你登录 GitHub 授权——按提示登录、授权即可。如果卡住或报错,最简单的替代方案是下载 [GitHub Desktop](https://desktop.github.com/)(图形界面工具,不用记命令),打开它、选择这个项目文件夹,点 "Publish repository" 按钮效果是一样的。

### Step 4:去飞书建一个机器人,拿到 Webhook 地址(强烈建议)

这一步是让系统"发现新东西时能通知你"的关键。

1. 打开飞书,进入你想接收通知的群聊(没有的话新建一个群,哪怕群里只有你自己)
2. 点群右上角的设置图标(⚙️)
3. 找到 "群机器人" → "添加机器人" → 选择 "自定义机器人"
4. 起个名字,比如"AI新词雷达",安全设置先跳过不用配置(能更快上线,之后想更安全再回来加)
5. 点确定后,飞书会给你一个类似 `https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxxxxxx` 的地址,**复制它**

### Step 5:把 Webhook 地址配置到 GitHub(密钥配置)

1. 打开你在 Step 3 创建的仓库网页
2. 点上方 "Settings" 标签页
3. 左侧菜单找到 "Secrets and variables" → "Actions"
4. 点绿色 "New repository secret" 按钮
5. **Name** 填 `FEISHU_WEBHOOK_URL`,**Value** 粘贴刚才复制的飞书地址
6. 点 "Add secret"

如果你还想启用 Replicate / Together AI / Fireworks AI / Artificial Analysis / Product Hunt 这几个需要密钥的源,去对应网站免费注册账号拿到 API Key(每家流程类似:注册 → 进入账号设置或 Dashboard → 找 "API Keys" 页面 → 创建一个),然后重复上面的步骤,分别新建一个 secret:

| Secret 名字(必须完全一致) | 去哪申请 |
|---|---|
| `REPLICATE_API_TOKEN` | replicate.com 注册后,账号设置里的 "API tokens" |
| `TOGETHER_API_KEY` | api.together.ai 注册后的 Dashboard |
| `FIREWORKS_API_KEY` | fireworks.ai 注册后的 API Keys 页面 |
| `FIREWORKS_ACCOUNT_ID` | fireworks.ai 账号设置里能看到你的 Account ID |
| `ARTIFICIAL_ANALYSIS_API_KEY` | artificialanalysis.ai/data-api 免费申请 |
| `PRODUCTHUNT_TOKEN` | producthunt.com/v2/oauth/applications 创建一个应用拿 token |

**这些全部可选,不申请也完全能跑**,只是对应那个源会被跳过。建议先跳过这一步,只配好飞书就上线,以后想解锁更多源再回来加。

### Step 6:手动触发一次,确认真的跑通了

1. 回到仓库网页,点上方 "Actions" 标签页
2. 左侧会看到一个叫 "AI Word Radar" 的工作流,点进去
3. 右边有个 "Run workflow" 下拉按钮,点开,再点绿色的 "Run workflow" 确认
4. 等 1-2 分钟,刷新页面,会出现一条新的运行记录——绿色对勾 ✅ 表示成功,红叉 ❌ 表示失败(点进去能看到具体哪一步、哪个日志出的问题)

跑成功之后,回到仓库主页,你会看到 `data/` 文件夹下多了一次自动提交(commit),说明数据被自动更新并存档了。

从这一刻起,**系统会每 20 分钟自动跑一次,你什么都不用做**。

### Step 7(可选):把网站部署上线

**方式一:先在自己电脑上预览(最简单,适合先看看效果)**

```bash
cd /Users/alex/Desktop/Tools/ai-word-radar/site
npm run dev
```

然后浏览器打开 `http://localhost:3000` 就能看到网站了。这种方式只有你自己电脑能看到,关掉终端就停了。

**方式二:部署到 Vercel(免费,任何人都能通过网址访问,且会随数据自动更新)**

1. 打开 [vercel.com](https://vercel.com),点 "Sign Up",选择用 GitHub 账号登录(一键完成,不用单独设密码)
2. 登录后点 "Add New" → "Project"
3. 找到并选择你的 `ai-word-radar` 仓库,点 "Import"
4. **关键一步**:找到 "Root Directory" 设置,点旁边的 "Edit",改成 `site`(因为网站代码在项目的 `site` 子文件夹里,不是仓库最外层)
5. "Framework Preset" 会自动识别成 "Next.js",不用改
6. 点 "Deploy",等 1-2 分钟

部署完成后,Vercel 会给你一个类似 `ai-word-radar-xxx.vercel.app` 的免费网址。以后每次 GitHub Actions 自动抓到新数据并提交,Vercel 会自动检测到、自动重新部署,网站内容跟着自动更新——你不需要手动做任何操作。

---

## 6. 日常怎么用

配置完成之后,你的日常使用方式非常简单:

1. **平时什么都不用管**——系统每 20 分钟自动跑一次。
2. **飞书群会通知你**——只有分数 ≥ 30 分的才会推送,所以群里安静是正常的,不代表系统没在工作。
3. 隔三差五打开 Vercel 网址,或者去仓库里看 `site/data/entities.json` / `data/scored.json` 这两个文件,扫一眼有没有高分的新发现。

**分数速查表**(对应你原来设计的那套规则):

| 分数 | 系统内部叫法 | 建议动作 |
|---|---|---|
| < 30 | log-only(仅记录) | 不用管,大概率是噪音(比如 Hugging Face 上一堆个人测试仓库) |
| 30~60 | reserve(先占位) | 值得多留意,可以考虑先注册个域名或者先写个单页 |
| 60~80 | launch-today(当天上线) | 值得当天就做个页面出来 |
| 80+ | full-site(全套上线) | 值得立刻做完整的专题站(首页/怎么用/价格/API/跑分/对比页那一整套) |

发现值得做的词之后,项目里已经有一个 `scoring/keywords.mjs` 模块,能自动帮你按你原方案生成三批关键词(发布当天用的词/使用需求词/竞争对比词)——这部分目前需要懂一点代码才能调用,以后如果需要我可以帮你接一个按钮或命令,让这步也变成"点一下就出结果"。

---

## 7. 常见问题 FAQ

**Q:Actions 页面显示很久没自动运行了,是不是坏了?**
A:GitHub 对超过 60 天没有任何提交的仓库,会自动暂停定时任务。但这套系统本身每次运行都会提交数据,只要它跑成功过一次,后续会持续保持"活跃"状态,不会被暂停。如果确实很久没跑,去 Actions 页面手动点一次 "Run workflow" 重新激活即可。

**Q:飞书群一直没收到消息,正常吗?**
A:非常正常。系统上线初期,大部分抓到的新条目分数都不高(比如 Hugging Face 上大量个人测试用的小仓库),真正值得关注的"大新闻"本来出现频率就不高。没消息 = 目前没有特别值得关注的新东西,不代表系统没在工作。

**Q:某个源一直显示抓取失败,要紧吗?**
A:不要紧。系统设计成"一个源失败不影响其他 26 个"。第 3 节列出的 ❌ 和 ⚠️ 状态都是已知情况,不是需要你立刻处理的报错。

**Q:我的 Mac 不开机,系统还会继续跑吗?**
A:会。数据抓取部分跑在 GitHub 的云端服务器上,跟你自己的电脑开不开机没有任何关系。你的 Mac 只在你想做后续开发/改代码的时候需要用到。

**Q:我想加一个新的监控源(比如某个我发现的新平台),怎么办?**
A:这需要写一点代码,规范写在 [CONTRACT.md](./CONTRACT.md) 里(每个监控源该怎么写、返回什么格式)。到时候可以把这个需求告诉我,我来帮你加。

**Q:飞书通知的内容能不能更丰富一点(比如带链接卡片而不是纯文字)?**
A:可以,现在 `notify/feishu.mjs` 用的是最简单可靠的纯文字消息。想升级成飞书的"卡片消息"样式(更好看、能点击跳转)也是可以做的,告诉我即可。

---

## 8. 以后还能怎么升级(不着急做)

这些都是"锦上添花"型的优化,现在不做完全不影响系统正常使用,想到了再做就行:

- 给评分引擎里目前留空的几项信号补上真实数据源:YouTube 教程出现检测、Google 自动补全检测、"是否只是单个博主转载"检测——这几项现在诚实地显示"未实现",不参与打分,不是随便瞎猜的。
- 把"发现新词 → 自动生成关键词矩阵 → 自动生成落地页草稿"这一整条链路完全打通,现在这几步之间还需要你手动跑一下命令。
- 给网站加上真正的域名和基础 SEO 设置(现在的 Vercel 免费域名能用,但正式运营建议换成自己的域名)。
- 优化 Reddit / AppSumo / Qwen 这几个目前不太稳定的抓取器,让它们更可靠。

有任何一项想现在就做,随时告诉我。
